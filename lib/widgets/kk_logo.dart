import 'package:flutter/material.dart';

class KKLogo extends StatelessWidget {
  const KKLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/logo1.png",
      height: 90,
    );
  }
}
